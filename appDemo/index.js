/**
 * @format
 */

import { AppRegistry } from "react-native";
import DatabaseComponent from "./components/DatabaseComponent";
import { name as appName } from "./app.json";

AppRegistry.registerComponent("appDemo", () => DatabaseComponent);
